# Binary search benchmark

`binary_search(values, target)` should return the index of `target` in a sorted list, or `-1` if absent.

**Intentional bug:** when the target is missing, the function returns a nearby index instead of `-1`.
